<?xml version="1.0" encoding="UTF-8"?>
<project name="${project.name}">

	<bannerLeft>
		<name>${project.name}</name>
	</bannerLeft>

	<publishDate format="yyyy-MMMM-dd HH:mm:ss" />

	<body>

		<!-- breadcrumb nav causes path errors in child projects -->
		<links>  
		 
		</links> 
		
		<!-- reports -->
		${reports}

	</body>

</project>